Drop your ThatsBIS "received loot" CSV export here (Loot → Export → CSV, "received" rows).
The pipeline auto-picks the most recent *.csv and shows the items awarded on THIS week's raid night
(Loot tab). Optional — if no CSV is present the Loot tab simply hides.
